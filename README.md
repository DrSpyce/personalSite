# Personal Portfolio (Flask)

Simple single-page portfolio powered by Flask.

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## Run

```bash
flask --app app --debug run
```

Then open <http://localhost:5000>.

